{
  "version": 2,
  "buildCommand": "ls -la && pwd && cd frontend && npm ci && npm run build:prod",
  "outputDirectory": "frontend/dist/frontuna-app/browser",
  "installCommand": "ls -la && pwd && cd frontend && npm ci"
}
